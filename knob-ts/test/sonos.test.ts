// import { Sonos } from '../src/outputs/sonos';

xdescribe('Sonos', () => {
  it('creates a new Sonos', () => {
    /**
     * @fixme How do you mock a Sonos device?
     */
    // const sonos = new Sonos();
    // expect(sonos).toBeDefined();
  });
});
