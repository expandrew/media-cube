server {
    listen      80;
    listen      [::]:80;
    server_name phoscon.go.expandrew.com;

    # security
    include     nginxconfig.io/security.conf;

    # reverse proxy
    location / {
        proxy_pass http://10.0.1.10:8000/;
        include    nginxconfig.io/proxy.conf;
    }

    # additional config
    include nginxconfig.io/general.conf;
}